"""repositorykit — shared helpers for the repository delivery tools.

Single source of truth for *what counts as a repository page*, the layer/template
exclude lists, and markdown parsing helpers used by both `verify_template.py`
and `collect_links.py`.
"""

from . import pages  # noqa: F401
